apiVersion: v1
kind: Service
metadata:
  name: {{ include "emcap-api.fullname" . }}
  labels:
    {{- include "emcap-api.labels" . | nindent 4 }}
    component: api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emcap-api.selectorLabels" . | nindent 4 }}
    component: api
