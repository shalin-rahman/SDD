# Session memo — P20 W1 API (Section 1)

**Date:** 2026-06-14  
**Plan:** `plan/20-standard-entity-rollout.md` Section 1 · Wave W1  
**Tasks:** EMCAP-P20-T05, EMCAP-P20-T09  
**Constraint:** No commit (user feedback A7)

## Goal

Close W1 API gaps: parametrized system-metadata contract for all 11 registry entities, W1 module field standardization (WAREHOUSE/CUSTOMER status chip, LEAD enum status, CONTACT lead lookup), fixtures + CRM tests.

## What changed

| Area | Paths |
|------|-------|
| Parametrized contract | `platform/api/tests/test_entity_system_contract.py` |
| CRM field tests | `platform/api/tests/test_crm_entity_fields.py` |
| W1 fixtures | `platform/api/tests/fixtures/metadata/{warehouse,lead,contact}.{form,grid}.keys.json`; updated `customer.grid.keys.json` |
| Modules | `modules/inventory/module.py` (WAREHOUSE `status_field`), `modules/demo/module.py` (CUSTOMER), `modules/crm/module.py` (LEAD enum + CONTACT lookup) |
| BN labels | `platform/api/src/emcap/metadata/builder.py` `FIELD_BN_LABELS` |
| Regression | `platform/api/tests/test_system_fields.py` WAREHOUSE status assertion |

## Verification

```bat
cd platform\api
python -m pytest tests/test_entity_system_contract.py tests/test_system_fields.py tests/test_crm_entity_fields.py tests/test_inventory_e2e.py -q
```

**Result:** 76 passed

## Open follow-ups (W2+)

- **W2 API (P20-T12):** JOURNAL_ENTRY (`account_id` LOOKUP, `amount` CURRENCY), SALE (`total` CURRENCY, `terminal_id` LOOKUP), LEAVE_REQUEST (`employee_id` LOOKUP, `leave_type` ENUM) + `status_field` on each
- **W3 API (P20-T14):** ACCOUNT balance CURRENCY, TERMINAL/EMPLOYEE/remaining `status_field` + fixtures for non-W1 entities
- **CUSTOMER optional `code` field** — deferred from W1 module table
- **Web (P20-T10):** Karma fixture parity for W1 entities (backlog marked Done elsewhere — verify web assets mirror new API fixtures)
- **Screenshots:** P18-T03 WAREHOUSE, P18-T06 CRM
