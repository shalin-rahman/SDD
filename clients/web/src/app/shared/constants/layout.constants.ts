/** Shared layout breakpoint — shell sidenav and master–detail panes. */
export const MOBILE_BREAKPOINT = '(max-width: 1023px)';

export const SEARCH_DEBOUNCE_MS = 300;

export const DEFAULT_PAGE_SIZE = 10;
