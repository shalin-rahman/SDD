import 'package:flutter_test/flutter_test.dart';

import 'package:emcap_mobile/api/emcap_client.dart';

void main() {
  test('EmcapClient exposes SDD platform API methods', () {
    final client = createClient('http://localhost:8000');

    expect(client.login, isA<Function>());
    expect(client.getMenus, isA<Function>());
    expect(client.getFormMetadata, isA<Function>());
    expect(client.getGridMetadata, isA<Function>());
    expect(client.listRecords, isA<Function>());
    expect(client.getRecord, isA<Function>());
    expect(client.createRecord, isA<Function>());
    expect(client.updateRecord, isA<Function>());
    expect(client.deleteRecord, isA<Function>());
    expect(client.restoreRecord, isA<Function>());
    expect(client.syncSnapshot, isA<Function>());
    expect(client.listNotes, isA<Function>());
    expect(client.addNote, isA<Function>());
    expect(client.listWorkflowInstances, isA<Function>());
    expect(client.transitionWorkflow, isA<Function>());
    expect(client.delegateWorkflow, isA<Function>());
    expect(client.listReports, isA<Function>());
    expect(client.runReport, isA<Function>());
    expect(client.listDocuments, isA<Function>());
    expect(client.uploadDocument, isA<Function>());
    expect(client.syncChanges, isA<Function>());
    expect(client.subscribeRecordsStream, isA<Function>());
    expect(client.listAudit, isA<Function>());
    expect(client.listNotifications, isA<Function>());
    expect(client.sendNotification, isA<Function>());
    expect(client.getPermissions, isA<Function>());
    expect(client.getRoles, isA<Function>());
    expect(client.listDashboards, isA<Function>());
    expect(client.getHealth, isA<Function>());
    expect(client.getPlatformConfig, isA<Function>());
    expect(client.listTenants, isA<Function>());
    expect(client.createPaymentIntent, isA<Function>());
    expect(client.dispatchRestIntegration, isA<Function>());
    expect(client.setToken, isA<Function>());
    expect(client.setTenantId, isA<Function>());
    expect(client.getAuthProviders, isA<Function>());
    expect(client.setOnUnauthorized, isA<Function>());
    expect(client.clearSession, isA<Function>());
    expect(client.loginOAuth, isA<Function>());
    expect(client.enrollMfa, isA<Function>());
    expect(client.verifyMfa, isA<Function>());
    expect(client.startWorkflow, isA<Function>());
    expect(client.listReportRuns, isA<Function>());
    expect(client.getReportRun, isA<Function>());
    expect(client.getDocument, isA<Function>());
    expect(client.aiChat, isA<Function>());
    expect(client.aiSummarize, isA<Function>());
    expect(client.getWorkflowInstance, isA<Function>());
    expect(client.publishKafkaIntegration, isA<Function>());
    expect(client.invokeSoapIntegration, isA<Function>());
    expect(client.uploadSftpIntegration, isA<Function>());
    expect(client.graphqlQuery, isA<Function>());
    expect(client.getMe, isA<Function>());
    expect(client.assignRole, isA<Function>());
    expect(client.checkAuth, isA<Function>());
    expect(client.escalateWorkflows, isA<Function>());
    expect(client.evaluateWorkflowRule, isA<Function>());
    expect(client.listEntities, isA<Function>());
    expect(client.getMetrics, isA<Function>());
    expect(client.confirmPaymentIntent, isA<Function>());
    expect(client.listAdminUsers, isA<Function>());
    expect(client.getAdminUser, isA<Function>());
    expect(client.createAdminUser, isA<Function>());
    expect(client.updateAdminUser, isA<Function>());
    expect(client.deactivateAdminUser, isA<Function>());
    expect(client.listAdminRoles, isA<Function>());
    expect(client.createAdminRole, isA<Function>());
    expect(client.updateAdminRole, isA<Function>());
    expect(client.getAdminSettings, isA<Function>());
    expect(client.updateAdminSettings, isA<Function>());
    expect(client.listAdminTemplates, isA<Function>());
    expect(client.createAdminTemplate, isA<Function>());
    expect(client.updateAdminTemplate, isA<Function>());
    expect(client.deleteAdminTemplate, isA<Function>());
    expect(client.getAdminAudit, isA<Function>());
    expect(client.getAdminSecurityPolicies, isA<Function>());
    expect(client.getAdminAbacPolicies, isA<Function>());
    expect(client.updateAdminAbacPolicies, isA<Function>());
    expect(client.getAdminIntegrations, isA<Function>());
    expect(client.updateAdminIntegrations, isA<Function>());
    expect(client.testAdminRestIntegration, isA<Function>());
    expect(client.updateAdminFieldAccess, isA<Function>());
    expect(client.getAdminLayoutMetadata, isA<Function>());
    expect(client.putAdminLayoutOverride, isA<Function>());
    expect(client.deleteAdminLayoutOverride, isA<Function>());
    expect(client.getTenantIsolationOps, isA<Function>());
    expect(client.putTenantIsolationOps, isA<Function>());
  });
}
