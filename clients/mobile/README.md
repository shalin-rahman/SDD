# Flutter mobile client

Metadata-driven UI. Shared contract in `lib/metadata_contract.dart`.

- `DynamicFormRenderer` / `DynamicGridRenderer` consume the same JSON as Angular.

Fetch metadata from platform API metadata endpoints.

## Analyze

```bash
cd clients/mobile
flutter pub get
flutter analyze
```
